<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <!--
        Solo hasta Android 9. Desde Android 10, MediaStore con RELATIVE_PATH escribe en la
        carpeta de imagenes sin pedir nada: es el modelo de almacenamiento por ambito y evita
        pedir acceso a todo el disco para guardar una foto propia.
    -->
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <uses-feature android:name="android.hardware.camera.any" android:required="true" />

    <application
        android:label="PROPOR"
        android:supportsRtl="true"
        android:theme="@style/Theme.Propor">

        <activity
            android:name=".ProporActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.Propor">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
